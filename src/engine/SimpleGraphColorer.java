package engine;

import java.util.ArrayList;
import java.util.List;
import java.util.Map;


/**
 * Colors simple graphs (not a simple colorer of graphs). Well, when I say it 
 * 'colors' them, I really want to say 'label' since they are strings. Usually
 * mathematical colors are numbers. But labelling a graph is also numbering it.
 * 
 * @author maclean
 *
 */
public class SimpleGraphColorer {
    
    public List<Graph> colorGraph(Graph graph, Map<String, Integer> colors) {
        List<Graph> graphs = new ArrayList<Graph>();
        
        return graphs;
    }

}
